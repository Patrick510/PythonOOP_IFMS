last_id = 0

class Tarefa:
  def __init__(self,id ,titulo ,prioridade ,descricao=none ,concluida):

    self.id = id
    self.titulo = titulo
    self.prioridade = prioridade
    self.concluida = concluida
    self.descricao = descricao
    last_id += 1
    self.id = last_id

  def simples(self):
    return
      f"{self.id} : {self.titulo}"

  def detalhada(self):
    return self.id+self.titulo+self.descricao+self.prioridade+self.concluida

  def __lt__(self,other):
    if self.prioridade<other.prioridade:
    return true
